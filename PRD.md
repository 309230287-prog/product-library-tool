# PRD：二期新品增量查重

## 问题陈述

新品入库时无法自动判断是否与已有编码重复，人工判断慢且不稳定。

## 目标用户

商品库维护人员。输入新品（品牌/名称/规格/类别/单位/备注），系统自动翻译为商品画像后比对基准库，判断能否复用已有编码。

---

## 功能需求

### P0（必须有）

- **FR-001：新品翻译** — 输入新品接龙表行（品牌/名称/规格/类别/单位/备注），翻译为完整商品画像，品牌取自*品牌列不从名称提取
- **FR-002：基准库索引** — 将12440条一期结果预处理为可快速查询的索引
- **FR-003：四维匹配** — 品名、品牌、规格+加工、单位四个维度全部命中才建议复用
- **FR-004：结果分类** — 输出三种结果：建议复用（推荐SPUID）/ 需新增 / 待确认
- **FR-005：Excel输入输出** — 读取`新品新增接龙表.xlsx`，对无系统编码的行进行匹配，结果反写到第10列(系统名称)和第11列(系统编码)

### P1（应该有）

- **FR-101：异名表匹配** — 新品叫"凉瓜"能找到库里的"苦瓜"
- **FR-102：子串匹配** — 新品"土豆"+备注"切丝"能发现库里"土豆丝"
- **FR-103：多候选推荐** — 多个四维全过时优先推命中次数多、无抄码的编码

### P2（锦上添花）

- **FR-201：规格冲突自动检测** — 新品名称和描述的规格不一致时标记
- **FR-202：加工意图检测** — 从备注中检测加工动作前缀，不硬编码分类词

---

## 四维匹配规则

### 维度1：品名（core相同/异名/子串 + 状态兼容 + paren兼容）

| 匹配方式 | 条件 | 结果 |
|---------|------|------|
| 精确 | core完全相同 | 品名通过 |
| 异名 | core在SYNONYM_MAP中互为异名 | 品名通过 |
| 子串 | 一个core是另一个的子串，差异部分被备注解释 | 品名通过（标记待确认） |
| 状态兼容 | 括号/前缀的状态词相同或冗余 | 品名通过 |
| paren兼容 | 括号内容相同或差异仅由状态词造成 | 品名通过 |

### 维度2：品牌

品牌来源两端不同：
- 新品：`*品牌` 列（采购自填，直接取用，"无要求"=无品牌）
- 基准库：translator 从名称中提取（已有 brand 字段）

| 条件 | 结果 |
|------|------|
| 双方都无品牌 | 通过 |
| 品牌相同 | 通过 |
| 品牌不同 | 不通过 |

### 维度3：规格+加工

新品侧规格有三个来源，`resolve_spec` 汇总为统一 spec_core：
- `*规格` 列 → 等同于 desc_spec（主要来源）
- `*名称` 列 → name_spec（名称中混入的数字+单位）
- 备注列 → 加工意图检测

基准库侧：
- 描述字段 → desc_spec
- 名称字段 → name_spec
- 无备注 → 无加工意图

| 条件 | 结果 |
|------|------|
| 双方都无规格且无加工意图 | 通过 |
| 都有规格且spec_core相同 + 加工意图一致 | 通过 |
| 规格不同 | 不通过 |
| 一方有规格/加工一方没有 | 待确认（信息不对等） |

### 维度4：单位

| 条件 | 结果 |
|------|------|
| 完全相同 | 通过 |
| 不同 | 不通过 |

---

## 技术方案

### 技术选型

- Python 3 + openpyxl（读写Excel）
- 复用一期 translator.py（语义翻译）+ 新建 matcher.py（匹配引擎）
- 索引格式：JSON（~10MB，单次加载）

### 数据流

```
[商品库整理_结果.xlsx]
    ↓ build_index.py（翻译+索引）
[index.json]
    ↓
[新品新增接龙表.xlsx] → check_new.py → [新品新增接龙表.xlsx]
                         只处理62条无编码的行
                         反写 系统名称(col10) + 系统编码(col11)
```

### 文件/模块结构

```
编码整理/
├── scripts/
│   ├── translator.py   # 语义翻译器（已有，需改造）
│   ├── rules.py         # 规则引擎（一期，二期不动）
│   ├── matcher.py       # 四维匹配引擎（新建）
│   ├── build_index.py   # 基准库索引构建（新建）
│   └── check_new.py     # 新品查重主入口（新建）
├── 商品库整理_结果.xlsx   # 基准库
├── index.json             # 索引文件（生成）
└── 新品新增接龙表.xlsx     # 输入+输出（反写系统名称/编码）
```

---

## 范围与边界

### 在范围内

- 新品翻译（名称中提取规格、加工意图检测、paren_content保留）
- 基准库索引构建（12440条用新翻译器重新翻译）
- 四维匹配引擎（含异名、子串匹配）
- 结果输出（建议复用/需新增/待确认 + 推荐编码 + 详情）

### 不在范围内

- 一期基准库再清洗
- 自动创建新编码
- 销售数据分析
- 三期exe封装

---

## 风险与待确认

| 风险 | 缓解 |
|------|------|
| 子串匹配误判（"鱼香肉丝"vs"鱼香肉"） | 标记待确认，不自动复用 |
| 新品无分类时状态冗余判断失效 | 退化为严格比对 |
| 规格正则匹配不完整（单位转换1L≠1000ml） | 不做单位转换，不同视为不同 |
| 加工词漏配（新动词前缀） | 靠子串匹配间接暴露 |

---

## 成功标准

- [ ] 10条验收案例全部通过
- [ ] 12440条基准库全量翻译无报错
- [ ] 输出Excel行数 = 输入行数
- [ ] 四维全过的候选推荐命中次数最多的编码

---

## 验收方式

1. 用二期需求文档中的10条验收案例逐条测试
2. 用一期已知的41组括号差异案例验证 paren 不误判
3. 用一期品牌修复案例验证品牌不误判
4. 随机抽取真实新品数据人工抽检

---
*生成日期：2026-05-20*
