# 二期需求文档：新品增量防重复

## 背景

一期已完成现有12440条SPU的清洗去重标注。每天有新商品需要新增编码，如果没有查重机制，重复会重新积累。

二期的核心价值：**新品入库前，先检查库里有没有能复用的编码。有就直接用，没有再新增。**

## 核心思想

和一期一样，不是字符串匹配，是语义翻译后比较。

## 核心规则：四个维度全部命中才给编码

新品来了，翻译后拆成四个维度，和库里逐一比对。**四个维度全部通过，才建议复用编码。任意一个不通过，就不能复用。**

```
维度: 品名 | 品牌 | 规格+加工 | 单位

品名一致？ AND 品牌一致？ AND 规格等价？ AND 单位相同？
  ↓全部通过                                    ↓任意一个不通过
"建议复用编码 CXXXXX"                  "不能复用 → 告知原因 → 请新增"
```

**每一步比对都是语义翻译后比较，不是字符串匹配。**

> 分类不参与匹配——新品入库时可能还没有分类，分类是后续人工定的。

## 四个匹配维度

### 维度1：品名（含处理状态）

新品名称经语义提取后 → 核心品名 + 处理状态。和库里比对。

**品名一致的情况**：
- 核心品名完全相同（提取掉后缀、抄码、品牌、规格、状态后剩余的部分）
- 新品和库里在异名表中互为异名 → 同样视为品名一致
- **核心品名不完全相同，但存在包含关系，且差异部分能被备注解释** → 视同品名一致（见下方"子串匹配"规则）

**品名不一致的情况**：
- 核心品名不同，且不满足子串匹配规则 → 不匹配

**子串匹配规则**：

当一个核心品名是另一个的子串时，不在词表里硬编码，而是看差异部分能否在备注里找到解释：

```
库里"土豆丝" vs 新品"土豆" + 备注"切丝"
  → core差异 = "丝"
  → 备注"切丝"包含"丝" → 差异可解释 → 品名通过

库里"萝卜块" vs 新品"萝卜" + 备注"切块"
  → core差异 = "块"
  → 备注"切块"包含"块" → 差异可解释 → 品名通过

新品"土豆片" vs 库里"土豆" + 备注"切块"
  → core差异 = "片"
  → 备注"切块"不包含"片" → 差异不可解释 → 品名不通过
```

- 新品core短、库里core长：看新品备注能否解释多的部分
- 新品core长、库里core短：看新品备注能否解释多出来的部分
- 差异无法解释 → 品名不通过
- 差异可解释 → 品名通过，但标记为"待确认"（因为子串匹配有误判风险，如"鱼香肉丝"vs"鱼香肉"，"丝"被当成了加工词）

**处理状态兼容性**：同上节，不变。

### 维度2：品牌

新品翻译时提取的品牌词。和库里比对。

- 品牌词一致 → 通过
- 双方都无品牌词 → 通过
- 品牌词不同（安井 ≠ 三全）→ **不通过**

### 维度3：规格+加工

从新品描述和备注中提取的规格及加工信息。和库里的描述比对。

**规格来源**（三个来源，按优先级取）：
1. 名称中语义识别提取的规格（如"300g安井鱼豆腐"→规格=300g）
2. 描述字段（如"500ml"、"1*4*2.5kg"）→ 翻译成 spec_core
3. 备注字段中认识的加工词（切丝、切块、切段、切丁、去籽、去头、去皮、单冻等）

如果名称和描述都有规格但不同 → 规格冲突，标记"需人工确认"。

**备注的处理**：备注是客户自由写的，内容不可控，可能含加工信息、配送信息（送二楼）、急单标记等。只提取认识的部分，不认识的原样保留不参与匹配。

**一致的情况**：
- spec_core 翻译后相同（如 1*500g → 500g → 500g）
- 都没写规格且都没加工要求
- 空≈抄码≈散称（都没固定规格）
- 都有加工要求且内容一致（都=切丝）

**不一致的情况**：
- 规格量级不同（1.65L ≠ 500ml）→ **不通过**
- 加工方式不同（切丝 ≠ 切块）→ **不通过**
- 一个有规格/加工一个没有 → **待确认**（信息不对等，不自动判）

### 维度4：单位

新品单位和库里单位直接比对。

- 单位相同 → 通过
- 单位不同（斤≠件≠包≠箱≠个）→ **不通过**

---

## 匹配结果分类

| 四个维度状态 | 结果 |
|------------|------|
| 四维全过（含抄码互换） | 建议复用编码 CXXXXX |
| 品名通过异名，其他三维通过 | 异名同物 → 建议复用编码 |
| 品名通过子串匹配，其他三维通过 | **待确认**（子串匹配有误判风险） |
| 品名不通过（状态冲突/品牌不同/规格不同/单位不同） | **需新增**，告知哪个维度失败 |
| 规格一个有一个没有（品名已通过） | **待确认** |
| 品名不通过（异名也找不到） | **请新增编码** |

---

## 输入输出

### 输入：Excel模板

新品统一填入 `新品待查模板.xlsx`：

| 列 | 字段 | 说明 |
|----|------|------|
| A | 名称 | 必填 |
| B | 单位 | 必填 |
| C | 描述 | 选填，规格信息 |
| D | 备注 | 选填，客户自由文本 |

### 输出：原Excel右侧追加3列

| 列 | 字段 | 内容 |
|----|------|------|
| E | 匹配结果 | 建议复用 / 需新增 / 待确认 |
| F | 建议编码 | 最佳匹配的 CXXXXX，无则为空 |
| G | 匹配详情 | 全部候选，每条的四维比对状态 + 综合判断 |

**如果有多个命中**，G列列出所有匹配的编码，每行一个，标注四维评价：

```
命中 3 条：
  C34286990(冬枣)      品名✓ 品牌✓ 规格✓ 单位✓ → 最佳匹配（正常版）
  C33870283(冬枣抄码)  品名✓ 品牌✓ 规格✓ 单位✓ → 同样是抄码版
  C33870282(冬枣抄码)  品名✓ 品牌✓ 规格✓ 单位✓ → 同样是抄码版
→ 综合判定：建议复用 C34286990
```

**匹配说明格式（5种）**：

```
四维全过：
  "品名、品牌、规格、单位与 CXXXXX(商品名) 均一致，建议复用该编码"

四维全过（抄码互换）：
  "品名、品牌、规格、单位与 CXXXXX(商品名) 均一致，抄码名→正常名，建议复用该编码"

四维全过（异名同物）：
  "品名通过异名(苦瓜=凉瓜)，品牌、规格、单位与 CXXXXX(商品名) 均一致，建议复用该编码"

某维不通过：
  "与 CXXXXX(商品名) 品名、品牌、单位一致，但规格不同(1.65L≠500ml)，需新增编码"

子串匹配通过：
  "品名通过子串匹配(库里\"土豆丝\"→新品\"土豆\"，差异\"丝\"被备注\"切丝\"解释)，品牌、规格、单位一致，需人工确认"

待确认（规格/加工不对等）：
  "品名、品牌、单位与 CXXXXX(商品名) 一致，但规格待确认(新品有加工切丝，库里无)，需人工确认"
```

### 输出示例

| 名称 | 单位 | 描述 | 备注 | 匹配结果 | 建议编码 | 匹配说明 |
|------|------|------|------|---------|---------|---------|
| 冬枣抄码 | 斤 | | | 建议复用 | C34286990 | 四维一致，品名、品牌、规格、单位与 C34286990(冬枣) 均一致，抄码名→正常名重复。库里有多个冬枣编码，优先推荐正常版 |
| 兰花一品鲜 | 瓶 | 1.65L | | 建议复用 | C36446180 | 四维一致，品名、品牌、规格、单位与 C36446180(兰花一品鲜) 均一致，该编码规格即为1.65L |
| 安井虾饺 | 件 | | | 需新增 | | 品名一致(虾饺)，单位一致(件)，但品牌不同(安井≠海霸王) |
| 苦瓜 | 斤 | | | 建议复用 | C39422350 | 四维一致，直接匹配库里"苦瓜"。同时提示库里有异名商品：凉瓜(C35049943) |
| 土豆 | 斤 | | 切丝 | 待确认 | | 品名、品牌、单位一致，但规格待确认：新品含加工要求(切丝)，库里土豆无此加工要求 |
| 土豆 | 斤 | | 切丝 | 待确认 | (见说明) | 库里"土豆丝"：品名通过子串匹配(土豆丝→土豆，差异"丝"被备注"切丝"解释)，标记待确认(子串匹配有误判风险) |

---

## 验收标准

| # | 新品(名称/单位/描述/备注) | 预期结果 | 说明 |
|---|-------------------------|---------|------|
| 1 | 冬枣抄码/斤// | 建议复用→C34286990 | 抄码名→正常名，四维全过。库里有冬枣(正常版)和冬枣抄码(抄码版)，优先推荐正常版 |
| 2 | 三角龙骨/斤// | 建议复用→C35203696 | 四维全过，库里有两条三角龙骨(C35203696,C35313274)，任意推荐一条 |
| 3 | 兰花一品鲜/瓶/1.65L/ | 建议复用→C36446180 | 四维全过，库里C36446180的spec就是1.65L |
| 4 | 草鱼（杀好）/斤// | 建议复用→C33870146 | 抄码名重复(草鱼杀好←抄码版)，四维全过(空≈抄码) |
| 5 | 冬枣/件// | 需新增 | 品名命中(冬枣)但单位不同(件≠斤)，库里的冬枣单位都是"斤" |
| 6 | 苦瓜/斤// | 建议复用→C39422350 | 直接匹配库里"苦瓜"，四维全过。同时提示异名：凉瓜(C35049943) |
| 7 | 阿根廷红虾500g/盒/500g/ | 需新增 | 提取后core=阿根廷红虾，spec=500g。品名不在库，异名表也没有 |
| 8 | 安井虾饺/件// (假设库里有海霸王虾饺/件) | 需新增 | 品名一致(虾饺)，单位一致(件)，但品牌不同(安井≠海霸王) |
| 9 | 土豆/斤// (备注=切丝) (库里有土豆/斤) | 待确认 | 品名、品牌、单位通过，规格：新品有加工(切丝)库里无 |
| 10 | 土豆/斤// (备注=切丝) (库里有"土豆丝"/斤) | 待确认 | 品名通过子串匹配(差异"丝"被备注解释)，其他三维一致，子串匹配有误判风险 |

---

## 翻译器二期改造

一期的翻译器是按位置剥离（前缀=品牌、末尾=规格、括号=状态）。但名称的写法不固定，"300g安井鱼豆腐"和"安井鱼豆腐300g"是一样的东西，按位置剥结果不同。

二期的翻译器必须从**"位置剥离"升级为"语义类型识别提取"**：不预设品牌在哪儿、规格在哪儿，而是扫描整个名称，识别出每个片段的语义类型，剩下的才是核心品名。

```
"安井鱼豆腐300g"     → 扫描 → {品牌=安井, 核心=鱼豆腐, 规格=300g}
"300g安井鱼豆腐"     → 扫描 → {品牌=安井, 核心=鱼豆腐, 规格=300g}
"鱼豆腐（安井）300g" → 扫描 → {品牌=安井, 核心=鱼豆腐, 规格=300g}

→ 三种写法，提取结果完全一致。
```

### 提取优先级

扫描名称时，按以下优先级识别：

| 优先级 | 类型 | 识别方式 | 示例 |
|--------|------|---------|------|
| 1 | 老编码后缀 | 正则 `-AA数字` 等 + 末尾残留【 | 冬枣-AA450582→已识别 |
| 2 | 抄码标记 | 含"抄码"二字 | 冬枣抄码→已识别 |
| 3 | 品牌词 | 和品牌表匹配，名称任意位置 | 安井/三全/海天→已识别 |
| 4 | 规格 | 数字+单位模式，名称任意位置（不限定开头/末尾） | 300g/500ml/1*6瓶→已识别 |
| 5 | 处理状态 | 和状态表匹配，括号内或前缀 | 杀好/活/冻/鲜/干→已识别 |
| 6 | 剩余文本 | 去除非语义空白后就是核心品名 | 鱼豆腐→品名 |

### 提取后的NameMeaning结构

```
NameMeaning
├── core: str           # 核心品名（去掉1-5后剩余）
├── brand: str|None     # 提取到的品牌词
├── name_spec: str|None # 名称中提取的规格（如果有）
├── has_chaoma: bool    # 是否含抄码
├── has_status: str|None # 处理状态
└── raw: str            # 原始名称
```

### 规格的最终确定

新品规格从三个来源汇集（名称提取 + 描述字段 + 备注加工），取法：

- 名称规格和描述规格一致 → 合并为一个规格值
- 名称规格和描述规格都有但不同 → 冲突，标记"需人工确认"
- 只有一方有规格 → 使用该值
- 都没有规格 → 无规格
- 备注中的加工信息 → 附加在规格维度，不参与 spec_core 比较，单独比对加工类型的语义等价

### 加工词归一化

备注中的加工描述不依赖字符串匹配，而是语义归一化：

| 原始写法 | 归一化结果 |
|---------|-----------|
| 切丝 | 加工=切丝 |
| 切2CM段 | 加工=切段, 尺寸=2cm |
| 切段2cm | 加工=切段, 尺寸=2cm |
| 切2cm段 | 加工=切段, 尺寸=2cm |
| 切块 | 加工=切块 |
| 去籽 | 加工=去籽 |

比对时比较的是归一化后的**加工类型**（切丝/切段/切块/去籽…），尺寸如果一致则完全匹配，尺寸不同提示差异，只有加工类型相同尺寸不同算"部分一致"。

### 基准库预处理

查重基准库不能用一期结果直接当索引。需要用新翻译器**重新处理所有12440条**商品，提取出每条的品牌、规格、状态，建立可四维比对的索引。

```
一期结果xlsx → 新翻译器重新处理 → 建立内存索引
索引字段: spuid, core, brand, spec, unit
```

---

## 复用一期产物

| 一期产物 | 二期用途 | 是否需要改造 |
|---------|---------|------------|
| `translator.py` | 翻译新品 | **需要改造**（上述4点） |
| `translator.py` (SYNONYM_MAP) | 异名匹配 | 不用改 |
| `translator.py` (BRAND_WORDS / PREFIX_STATUS / PAREN_STATUS) | 品牌、状态提取 | 不用改 |
| `商品库整理_结果.xlsx` | 查重数据源 | 需要重新通过新翻译器处理，提取每条的四维字段 |

## 与一期区别

| 维度 | 一期 | 二期 |
|------|------|------|
| 模式 | 全量12440条标注 | 增量逐条查重 |
| 匹配 | 优先级链 | 四个维度全通过 |
| 输出 | 每条一个标注结果 | 每条多个匹配候选 |
| 分类 | 参与匹配 | 不参与 |
| 品牌 | 剥离但不记录 | 语义提取品牌，独立比对维度 |
| 翻译器 | 位置剥离（前缀=品牌、末尾=规格） | 语义类型识别提取（不依赖位置） |
| 基准库 | 标注结果直接使用 | 需重新翻译，建立四维索引 |
