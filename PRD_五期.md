# PRD：五期 — 规则引擎 + DeepSeek 复核

## 问题陈述

一期清洗的规则引擎在语义歧义场景下存在误判，需要 DeepSeek 对规则引擎标记的疑似重复商品做最终语义复核。

## 目标用户

使用商品库工具 v2.0 进行清洗的用户（内部运营/采购人员），期望清洗结果准确。

## 功能需求

### P0（必须有）

- **FR-501**：规则引擎跑完后，收集标记为"完全重复""抄码名重复""异名同物"的行
- **FR-502**：按组号（group_id）分组，同一组商品一起送审
- **FR-503**：每组调用 1 次 DeepSeek API，判断组内商品是否为同一商品
- **FR-504**：DeepSeek 判定"是同一商品"→ 保持原标记不变
- **FR-505**：DeepSeek 判定"不是同一商品"→ 整组全部改回"正常"，清空组号
- **FR-506**：标记为"缺省值"的行不参与复核，保持不变
- **FR-507**：API 调用异常（超时/报错）时该组标记为"待确认"，不阻塞整体流程

### P1（应该有）

- **FR-508**：控制台输出复核进度（如 `[复核] 15/312 组完成`）
- **FR-509**：复核完成后输出统计（总组数、判定为同一商品组数、判定为不同商品组数、异常组数）

## 不涉及

- 不改动模块B（新品查重）的判断逻辑
- 不改动四期前端和后端路由
- 不重新设计规则引擎
- 不做全量 12440 条的 DeepSeek 判断
- 不修 rules.py 里的规则逻辑
- 不改动 clean_process 的列读取逻辑（四期已修）

## 技术方案

### 改动位置

**`web/app.py`** — `clean_process()` 函数末尾，规则引擎执行完毕后，追加复核步骤。

**`scripts/judge.py`** — 新增 `review_group()` 函数，封装 DeepSeek 调用。

### 数据流

```
clean_process()
  │
  ├─ Step1: 读 Excel → translator 翻译 → ProductRow 列表
  │
  ├─ Step2: RuleEngine.run_all()
  │    规则引擎逐级执行：
  │      run_quesheng()  → 标记"缺省值"（229条）
  │      run_wanquan()   → 标记"完全重复"（387条，分组号重N）
  │      run_chaoma()    → 标记"抄码名重复"（256条，分组号抄N）
  │      run_yiming()    → 标记"异名同物"（12条，分组号异N）
  │      run_normal()    → 剩余标记"正常"（11556条）
  │
  └─ Step3: DeepSeek 复核阶段（新增）
        │
        ├─ 3.1 收集所有行，筛选 anomaly_class ∈
        │    {"完全重复", "抄码名重复", "异名同物"}
        │    排除 anomaly_class = "缺省值" 和 "正常"
        │
        ├─ 3.2 按 group_id 分组
        │    例：{"重103": [row1, row2], "抄38": [row3, row4], ...}
        │    得到 N 组（约 300 组）
        │
        ├─ 3.3 逐组处理：
        │    for each group_id, items in groups.items():
        │      result, reason = judge.review_group(items, model)
        │      → result=True:
        │           保持原标记，跳过
        │      → result=False:
        │           组内每行：anomaly_class="正常", group_id="", suggestion=""
        │      → result=None:
        │           组内每行：anomaly_class="待确认", suggestion=reason
        │
        └─ 3.4 输出汇总统计
```

### 关键数据变换

#### 分组阶段

规则引擎跑完后，`task_state['clean_results']` 中的数据示例：

```json
[
  {"spuid": "C33882790", "name": "黄骨鱼抄码-EE152717", "unit": "斤",
   "anomaly_class": "完全重复", "group_id": "重103", "suggestion": "与C33884407..."},
  {"spuid": "C33884407", "name": "黄骨鱼（杀好）抄码-EE337905", "unit": "斤",
   "anomaly_class": "完全重复", "group_id": "重103", "suggestion": "与C33882790..."},
  {"spuid": "C36302853", "name": "黄骨鱼（杀好）-EE1469749", "unit": "斤",
   "anomaly_class": "抄码名重复", "group_id": "抄38", "suggestion": "保留正常名编码..."},
  {"spuid": "C33882790", "name": "黄骨鱼抄码-EE152717", "unit": "斤",
   "anomaly_class": "抄码名重复", "group_id": "抄38", "suggestion": "停用抄码名编码..."}
]
```

按 group_id 分组后：

```python
{
  "重103": [
    {"name": "黄骨鱼抄码-EE152717", "unit": "斤", "desc": "抄码", "category": "水产类"},
    {"name": "黄骨鱼（杀好）抄码-EE337905", "unit": "斤", "desc": "抄码", "category": "水产类"}
  ],
  "抄38": [
    {"name": "黄骨鱼抄码-EE152717", "unit": "斤", "desc": "抄码", "category": "水产类"},
    {"name": "黄骨鱼（杀好）-EE1469749", "unit": "斤", "desc": "抄码", "category": "水产类"}
  ],
  ...
}
```

**注意**：同一个 SPUID 可能出现在多个组中（如 C33882790 既在重103又在抄38）。每组独立判断，互不影响。

#### DeepSeek 判断阶段

**重103 的请求：**
```
你是一名商品数据审核员。请判断以下多个商品信息是否指向同一个实际商品。
判断依据：名称含义、规格、单位、分类。

商品列表：
1. 名称：黄骨鱼抄码，单位：斤，描述：抄码，分类：水产类
2. 名称：黄骨鱼（杀好）抄码，单位：斤，描述：抄码，分类：水产类

请仅回答 "yes" 或 "no"。
```
→ 期望返回 `no`（一个有杀好一个没有，不同商品）

**抄38 的请求：**
```
商品列表：
1. 名称：黄骨鱼抄码，单位：斤，描述：抄码，分类：水产类
2. 名称：黄骨鱼（杀好），单位：斤，描述：抄码，分类：水产类
```
→ 期望返回 `yes`（抄码名 vs 正常名，同一商品）

#### 结果写回阶段

```python
# 重103 → DeepSeek 返回 "no"
for row in rows:
    if row.spuid in ('C33882790', 'C33884407') and row.group_id == '重103':
        row.anomaly_class = '正常'
        row.group_id = ''
        row.suggestion = ''

# 抄38 → DeepSeek 返回 "yes"
# 不修改，保持抄码名重复
```

### judge.py 新增函数设计

```python
def review_group(items: list[dict], model: str = "deepseek-chat") -> tuple[bool | None, str]:
    """
    判断同组商品是否为同一商品。
    
    参数:
        items: [
            {"name": "黄骨鱼抄码", "unit": "斤",
             "desc": "抄码", "category": "水产类"},
            ...
        ]
        model: 模型名，默认 deepseek-chat
    
    返回:
        (True, "")         = 是同一商品（保持原标记）
        (False, "")        = 不是同一商品（改回正常）
        (None, "原因")     = API 异常（标为待确认，原因用于 detail）
    
    说明:
        - 使用全局 API_KEY / API_URL 配置
        - 超时时间 30 秒
        - 内置重试逻辑：失败时重试 1 次
    """
```

### 提示词设计

```
你是一名商品数据审核员。请判断以下多个商品信息是否指向同一个实际商品。

判断规则：
- 名称核心含义相同 + 单位相同 + 分类相同 → 是同一商品
- 只有加工状态不同（杀好/切片/冻品/去内脏等）→ 仍是同一商品
- 名称核心含义不同 → 不是同一商品
- 部位/品种不同（二刀肉 vs 普通腊肉，花鳝 vs 普通鳝鱼）→ 不是同一商品
- 规格/容量/重量不同（1kg vs 5kg）→ 不是同一商品

商品列表：
{items}

请仅回答 "yes" 或 "no"。
```

说明：名称送 DeepSeek 之前需要先去掉老编码后缀（如 `-EE152717`、`-CC2536839【`），保留有意义的文本。去编码规则：`re.sub(r'[-][A-Z]{2}\d+[【〔]?.*', '', name)`。

### 错误处理

#### API 异常场景

| 异常类型 | 表现 | 处理方式 |
|----------|------|---------|
| 网络超时（>30s） | requests.exceptions.Timeout | 重试 1 次，仍失败 → 该组标"待确认" |
| 返回非 JSON | 解析失败 | 标"待确认"，detail="DeepSeek返回格式异常" |
| 返回内容不是 yes/no | 内容不匹配 | 标"待确认"，detail="DeepSeek返回内容异常: {原始内容}" |
| HTTP 错误（4xx/5xx） | requests.exceptions.HTTPError | 标"待确认"，detail="API错误: {状态码}" |

#### 原子性

- 一组内所有行要么全改要么全不改，不允许部分修改
- 修改直接在 `task_state['clean_results']` 的 dict 对象上操作，不需要重建列表
- 复核只改 `anomaly_class`、`group_id`、`suggestion` 三个字段

### 进度输出

```
[清洗] 12440/12440
[复核] 开始, 共 312 组
[复核] 1/312 重103 → no (不同商品)
[复核] 2/312 重104 → yes (同一商品)
[复核] 3/312 重105 → yes (同一商品)
...
[复核] 312/312 抄56 → yes (同一商品)
[复核] 完成: 312组, yes=298, no=12, 异常=2
```

## 范围与边界

### 在范围内

- clean_process 末尾追加 DeepSeek 复核
- 按组号分批调用，每组 1 次
- 缺省值不动
- 控制台输出进度

### 不在范围内

- 前端 UI 改动（不新增进度条/状态提示）
- 规则引擎逻辑改动
- 模块B相关代码

## 数据完整性保障

| 检查项 | 保障方式 |
|--------|---------|
| 行数不变 | 复核只改 anomaly_class/group_id/suggestion，不增删行 |
| 缺省值不变 | 229 条缺省值不参与任何复核逻辑 |
| 正常行不变 | 11556 条正常行不参与复核 |
| 一组内原子性 | 要么全改要么全不改，不会出现组内部分修改 |
| API 异常不丢失数据 | 异常组标"待确认"，detail 保留原因，用户可追溯 |

## 风险与待确认

### 风险

| 风险 | 影响 | 缓解 |
|------|------|------|
| DeepSeek API 不稳定 | 复核中断或部分组失败 | 逐组异常捕获，失败组标"待确认"+重试1次 |
| API 调用耗时 | 清洗时间变长（约 300次×2s=10min） | 控制台打印进度，用户可见 |
| DeepSeek 误判 | 把真重复改回正常 | 标"待确认"组可人工审查 |
| 同一个 SPUID 在不同组中被 DeepSeek 给出矛盾判断 | 同一个商品在A组判不同、在B组判相同 | 每组独立判断，互不影响，结果以组为单位 |

### 待确认

- [ ] DeepSeek 用哪个模型？（三期用的是 deepseek-chat，复用？）
- [ ] 并发调用还是串行调用？—— 建议串行（避免触发限流）

## 成功标准

- [ ] 重103（黄骨鱼抄码 vs 黄骨鱼（杀好）抄码）→ 判定为不同商品 → 改回正常
- [ ] 重111（湖南腊肉抄码 vs 湖南腊肉（二刀肉）抄码）→ 判定为不同商品 → 改回正常
- [ ] 抄38（黄骨鱼抄码 vs 黄骨鱼（杀好））→ 判定为同一商品 → 保持抄码名重复
- [ ] 缺省值 229 条不受复核影响
- [ ] 12440 条总数不变
- [ ] API 调用量 ≤ 组数（约 300 次）

## 验收方式

1. 浏览器打开 Web 工具 → 模块A → 上传 `商品库整理.xlsx`
2. 等待清洗完成（含 DeepSeek 复核），观察控制台输出：
   ```
   [复核] 开始, 共 N 组
   [复核] 1/N 重103 → no
   [复核] 2/N 重111 → no
   ...
   [复核] N/N 抄38 → yes
   ```
3. 检查结果：
   - 重103 和 重111 行全部为"正常"
   - 抄38 仍保留"抄码名重复"
   - 缺省值 229 条不变
   - 总条数仍是 12440
4. 导出清洗结果 → Excel 验证上述条件

---
*基于 PROBLEM_五期.md 生成，日期：2026-05-27*
