# 需求文档：商品库整理清洗 v3（语义理解版）

## 核心思想

**SPU名称、单位、描述、分类，四者是一段完整的话，描述"这是个什么东西、怎么卖的"。
去重不是匹配字符串，是翻译后比较意思。**

```
"冬枣抄码-AA450582 / 斤 / 抄码 / 水果类"
    ↓ 翻译
东西是冬枣，称重卖，老编码不管，水果类

"冬枣 / 斤 / (空) / 水果类"
    ↓ 翻译
东西是冬枣，没写规格，水果类

→ 意思一样 → 重复
```

## 数据

- 文件：`商品库整理.xlsx`
- 12440行，列1-5有数据，列6-9待填写

## 翻译规则

### 名称字段：翻译成"这是什么"

名称里混了品牌、方言名、老编码后缀、处理状态等多种信息。翻译时不是全部剥离，而是区分：

**必须剥离的（它不是商品本身）：**

1. 老编码后缀（`-AA数字`、`-BB数字`等）→ 旧体系残留，直接砍掉
   - 注意：后缀后面可能还跟了规格信息（如 `-BB9233773-1*10*0.9kg`），砍掉后缀后规格保留在名称中
   - 注意：末尾残留的`【`一并砍掉（如 `抄码-BB6832860【`）
2. "抄码" → 它是规格描述（称重），不是商品名成分
3. 品牌词（安井、三全、海霸王、稻香诚制等）→ 品牌不是"它是什么"，无论出现在名称的什么位置都剥离
   - 品牌在开头："安井爆浆糍粑" → 东西=爆浆糍粑
   - 品牌在中间："150G光明健能原味酸牛奶" → 东西=原味酸牛奶
   - 品牌列表见脚本中的 BRAND_WORDS
4. 前缀状态词 → 名称开头可能带状态描述，它也不是商品本身
   - `冻`、`活`、`鲜`、`干`、`冰鲜`、`速冻`、`杀好`、`散装`、`散称`
   - "鲜牛腩" → 东西=牛腩，状态=鲜
   - "干发菜" → 东西=发菜，状态=干
   - "杀好鳊鱼" → 东西=鳊鱼，状态=杀好

**括号内容——存原文，不猜测，比对时区分是否"状态"：**

5. 括号里的内容（无论是什么），**原样保留**到 `paren_content` 字段，不丢弃不分类
   - 只把一小撮"状态词"同时记入 `has_status`：`杀好`、`鲜`、`活`、`冻`、`冰鲜`、`速冻`、`散装`、`散称`
   - 其他内容（大、小、去皮、去籽、去头…）**不进 has_status**
6. 比对规则：
   - `paren_content` 完全相同 → 品名维度可合并
   - `paren_content` 不同 → 检查差异是否仅由"状态词"造成
     - 是（如"杀好" vs None）→ 差异仅状态 → 可合并
     - 否（如"大"vs"小"、None vs"去皮"）→ 差异是规格/加工 → 不合并
7. 括号里的别名（如"鹌鹑蛋（鸟蛋）"）→ 是异名信息，在异名表里处理
8. 规格数字混在名称里的（如"安井爆浆糍粑 300g*24"）→ 提取到规格维度

```
翻译示例：
"沙虾（大）抄码" → core=沙虾, paren_content="大"
"沙虾（小）"     → core=沙虾, paren_content="小"
→ paren不同，"大""小"都不是状态词 → 不合并 ✓

"鲈鱼（杀好）抄码" → core=鲈鱼, paren_content="杀好", has_status="杀好"
"鲈鱼抄码"         → core=鲈鱼, paren_content=None
→ paren不同，但差的一方"杀好"是状态词 → 差异仅状态 → 可合并 ✓

"鲜竹笋（去皮）抄码" → core=鲜竹笋, paren_content="去皮"
"鲜竹笋抄码"         → core=鲜竹笋, paren_content=None
→ paren不同，"去皮"不是状态词 → 不合并 ✓
```

### 单位字段：翻译成"怎么卖"

斤 ≠ 包 ≠ 件 ≠ 箱 ≠ 个 ≠ 条，不同单位就是不同卖法，不算重复。

### 描述字段：翻译成"什么规格"

描述在说"这个商品的规格信息是什么"。翻译目标：提取可比较的规格语义。

**第一步：判断有没有写规格**
- 空、无、抄码、散称、散装 → 没写规格
- 描述内容等于单位字段（如单位=个，描述=个）→ 冗余信息，视为没写规格
- 其他 → 写了规格

**第二步：翻译规格的意思**
- 统一乘号：×、x、X → *
- 去掉描述外围的 `1*`
- `1*500g` → 500g | `1*4*2.5kg` → 4*2.5kg | `1*6瓶` → 6瓶
- 描述是纯文字（如"顺陶陶"）→ spec_core 保留原文字，但无法与规格比较
- 含逗号/分号的复合描述 → 取第一段为规格主语义，后半段视为附加说明（供应商/备注等）
  - `"30头，东门海连"` → 规格主语义=30头，附加说明=东门海连
- 规格主语义中如果混入了品牌词（如"澳皇360G"）→ 去掉已知品牌词后再比
- 方括号包裹（如`[1*500g]`）→ 去壳后正常翻译

**第三步：比较规格意思**

| 情况 | 判断 | 原因 |
|------|------|------|
| 都没写规格 | 等价 | 都相当于"无规格信息" |
| 都写了，翻译后相同 | 等价 | 同一个规格 |
| 都写了，翻译后不同 | 不等价 | 不同SKU（如500ml vs 1.65L） |
| 一个写了，一个没写 | **标记为"规格待确认"** | 信息不对等，不自动判，但有可能是重复（另一条漏填了规格） |

**翻译示例：**

| 描述原文 | 翻译结果 | 意思 |
|----------|---------|------|
| `空` | (无规格) | 没写 |
| `抄码` | (无规格) | 没写 |
| `无` | (无规格) | 没写 |
| `散称` | (无规格) | 散卖称重，没固定规格 |
| `散装` | (无规格) | 散装，没固定规格 |
| `1*500g` | 500g | 500克 |
| `500g` | 500g | 500克 |
| `1*4*2.5kg` | 4*2.5kg | 4份，每份2.5公斤 |
| `4*2.5kg` | 4*2.5kg | 4份，每份2.5公斤 |
| `1*6瓶` | 6瓶 | 6瓶 |
| `1.65L` | 1.65L | 1.65升 |
| `500ml` | 500ml | 500毫升 |
| `1*12*500ml` | 12*500ml | 12份，每份500毫升 |
| `30头，东门海连` | 30头 | 30头（忽略供应商备注） |
| `7*17.5kg，实际抄码` | 7*17.5kg | 7×17.5kg（忽略"实际抄码"备注） |

### 分类字段：翻译成"哪一类"

**基本规则**：分类相同或可互通，才算同一个东西。蔬菜类 vs 耗材类 → 绝不可能是同一物。

**互通分类对**：
- 蔬菜类 ↔ 蔬菜类（通用）
- 干调类 ↔ 干调类（通用）
- 冻品类 ↔ 冻品类（通用）
- 水产类 ↔ 水产类（通用）↔ 水产品
- 鲜肉类 ↔ 鲜肉类（通用）
- 禽蛋类 ↔ 禽蛋类（通用）↔ 家禽类
- 酒水副食类 ↔ 酒水副食类（通用）↔ 饮料副食类
- 粮油类 ↔ 粮油类（通用）
- 豆制品类 ↔ 豆制品类（通用）
- 水果类 ↔ 水果类（通用）

**分类写错的处理**：

实际数据中存在同一商品被分到不同分类的情况（如"可口可乐"一条在干调类、一条在酒水副食类）。分类不一致但名称+单位翻译后完全相同的，**不直接标重复，标记为"分类冲突_待确认"**。

**不同形态不同分类**：

同一商品名跨鲜/冻两类时（如"鸡边腿"鲜禽类 vs 冻品类），往往是不同形态，不算重复。

---

## 清洗分类体系（按优先级，互斥）

**核心约束**：
- 每行只属于一个异常分类，一旦被标记就不再参与后续匹配
- 一个商品可能和多个商品有重复关系，取最先匹配到的
- 处理状态（杀好、冻品等）不仅在名称括号里可能出现，**描述字段也可能出现**（如desc="杀好"），同样影响商品身份判断

### 第1级：缺省值

**语义**：名称或描述里含"缺省值"→ 数据不完整。

**判定**：
- 名称含"缺省值" → 直接标（44条）
- 描述含"缺省值"但名称不含 → 同样标，但说明="描述字段标记为缺省值"（约185条）

**动作**：组号="缺省"，异常分类="缺省值"，建议="补全信息或停用"

**预计**：约229条

---

### 第2级：完全重复

**语义**：两个及以上编码的商品，翻译后意思完全一样。只是不同时期/不同体系录了多次。

**判定条件**（全部满足）：
1. 名称翻译后相同（去后缀、去抄码、去品牌）
2. 单位相同
3. 分类相同或互通
4. 描述意思等价（都没写规格，或翻译后的规格相同）

**涵盖子类型**：
- 正常名 vs 正常名（老编码不同）
- 正常名 vs 带后缀名（如"潮州绿豆饼" vs "潮州绿豆饼-BB8511069"）
- **抄码名 vs 抄码名**（同一商品录了多个抄码编码，如"白菜仔（白菜苗）抄码"有AA和CC两个编码）→ 两组都没匹配到正常名时，它们之间也标完全重复

**前缀状态词冗余**：前缀状态词在该分类下属于冗余信息时，可以标重复：
- 鲜肉类里的"鲜X" → "鲜"冗余，鲜X = X（如"鲜牛腩" = "牛腩"在鲜肉类）
- 干调类里的"干X" → "干"冗余，干X = X（如"干发菜" = "发菜"在干调类）
- 水产类里的"冰鲜X" → "冰鲜"冗余，冰鲜X = X（如"冰鲜红杉鱼" = "红杉鱼"在水产类）
- 只有同分类才标冗余。跨分类的（如"冻X"在冻品类 vs "X"在水产类）→ 形态不同，不标

**名称翻译不相同的（不标）**：
- 一个带处理状态一个不带 → 不同东西（如"草鱼" vs "草鱼（杀好）"）
- 鲜vs冻 → 不同形态
- 杀好 vs 未杀好 → 不同（杀好改变了商品性质）

**描述一个写了一个没写的（不直接标）**：
- 标记为"规格待确认"，不直接入完全重复
- 另行输出待确认清单

**分类冲突的（不直接标）**：
- 名称+单位相同但分类不同且不互通 → 标记为"分类冲突_待确认"
- 不直接入完全重复

**动作**：组号="重N"，异常分类="完全重复"，说明="与XXX语义完全一致，重复编码"，建议="保留其一"

**预计**：约115-130组

---

### 第3级：抄码名重复

**语义**：同一商品有"称重版"和"普通版"两个编码。称重版名称里带了"抄码"。

**判定条件**（全部满足）：
1. A的名称含"抄码"
2. 把A名称里的"抄码"去掉后，翻译结果和B的名称翻译结果一致
3. 单位相同
4. 分类相同或互通

**动作**：组号="抄N"，异常分类="抄码名重复"，说明="抄码名版本，对应正常名编码XXX"，建议="停用抄码名编码"

**预计**：约115条

---

### 第4级：异名同物

**语义**：两个名称翻译后指同一个东西，只是叫法不同（方言/学名/俗称）。

**判定条件**（全部满足）：
1. 两个名称在异名表中，且互为异名
2. 单位相同
3. 分类相同或互通
4. 双方都不含处理状态差异

**异名表**：硬编码约110组异名对，覆盖叶菜、果菜、葱蒜、鱼、虾蟹、水果、干货、豆制品等。

**动作**：组号="异N"，异常分类="异名同物"，说明="同物异名: A = B"，建议="确定标准名称"

**预计**：约80-150条

---

### 第5级：正常

以上都不满足的 → 正常。

**义乌商品（444条）**：全部标正常，暂不处理。

---

### 第6级（额外输出）：待确认清单

以下情况不自动判，单独输出清单供人工复核：

| 类型 | 触发条件 | 输出内容 |
|------|---------|---------|
| 分类冲突 | 名称+单位相同，分类不同且不互通 | 双方编码、名称、分类 |
| 规格待确认 | 名称+单位+分类相同，一个写了规格一个没写 | 双方编码、名称、描述 |

---

## 验收案例

| SPUID | 翻译后的意思 | 预期分类 | 原因 |
|-------|-------------|---------|------|
| C33876377 | 本地公鸡，缺省值 | 缺省值 | 名称含"缺省值" |
| C35203696 | 三角龙骨，斤，没规格，鲜肉类 | 完全重复 | 与C35313274语义完全一致 |
| C35313274 | 三角龙骨-DD204294，斤，抄码(没规格)，鲜肉类 | 完全重复 | 与C35203696语义完全一致 |
| C34866279 | 一次性锡纸蛋挞托，条，没规格，耗材类 | 完全重复 | 与C34526252语义完全一致 |
| C34526252 | 一次性锡纸蛋挞托-LL182047，条，没规格，耗材类 | 完全重复 | 同上 |
| C33870283 | 冬枣抄码-AA450582，斤，没规格，水果类 | 抄码名重复 | 对应正常名C34286990 |
| C34286990 | 冬枣，斤，没规格，水果类 | 抄码名重复 | 正常名版本 |
| C36446180 | 兰花一品鲜，瓶，1.65L，干调类 | 正常 | 与C37047023规格不同(1.65L≠500ml) |
| C37047023 | 兰花一品鲜，瓶，500ml，干调类 | 正常 | 规格不同 |
| C43700141 | 义乌阿妈蛋饺，箱 | 正常 | 义乌不处理 |
| C34362518 | 可口可乐，件，干调类 | 分类冲突_待确认 | 与C34474836(酒水副食类)分类冲突 |
| （待找） | 草鱼（杀好），斤 | 正常 | 与"草鱼"处理状态不同 |
