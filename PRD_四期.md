# PRD：四期 — 完整 Web 工具

## 问题陈述

一期清洗去重 + 二期新品查重 是两个独立的命令行脚本，同事无法使用。需要整合为一个浏览器工具，两个标签页并列。

## 目标用户

商品库维护人员。浏览器操作，不装 Python，不碰命令行。

---

## 功能需求

### P0（必须有）

**FR-401：清洗去重模块（模块A，纯规则引擎）**

一期逻辑搬上 Web。不调用 DeepSeek，只用 translator.py + rules.py 做规则匹配。

- 用户点击或拖拽上传原始商品库 Excel（如 商品库整理.xlsx）
- 系统逐行翻译 + 建索引 + 规则引擎逐级清洗（缺省值→完全重复→抄码名重复→异名同物→正常）
- 清洗过程中显示进度条（每 200 条刷新）
- 完成后展示统计卡片：总条数 / 发现重复 / 正常 / 缺省值
- 结果表格列：SPUID / 名称 / 单位 / 描述 / 异常分类 / 组号 / 建议
- 异常分类颜色标签：完全重复(红色) / 抄码名重复(黄色) / 缺省值(橙色) / 正常(绿色)
- 导出按钮下载含标注列的 Excel（追加四列：组号 / 组说明 / 建议 / 异常分类）
- 重置按钮（仅影响模块A）清空所有结果，回到上传状态
- 文件格式校验：上传非 .xlsx 文件或列结构不符时，红色文字提示具体错误，不崩溃

**FR-402：新品查重模块（模块B，规则引擎 + DeepSeek 判断）**

二期逻辑 + DeepSeek judge 搬上 Web。

- Step1：上传清洗后的商品库 Excel → 系统翻译 + 建索引 dict → 进度条（每 200 条刷新）→ 完成后显示"索引就绪：N 条，M 个品类"
- Step2 联动：索引未就绪时锁定（灰色 + 禁止点击），显示"请先上传商品库建立索引"。Step1 完成后自动激活
- Step2 激活后：用户上传接龙表 Excel → 上传区域显示已选文件名和总条数
- 控制按钮状态机：
  - 初始：开始可用，暂停/停止禁用
  - 运行中：开始禁用，暂停/停止可用
  - 暂停中：开始禁用，暂停文字变为"继续"，停止可用
  - 完成后：开始可用，暂停/停止禁用
- 开始查重后，每行分两步：
  1. 规则引擎翻译新品 → 查索引找候选 → 异名表 → 子串匹配 → 候选列表
  2. 候选 ≥ 1：候选列表 + 新品画像 → DeepSeek API 判断 → 建议复用/需新增/待确认
     候选 = 0：直接判"需新增"，不调 DeepSeek，detail 写"库中未找到匹配"
- DeepSeek API 调用超时或报错 → 该行标记为"待确认"，detail 记录异常原因，继续处理下一行
- 进度条显示当前处理进度（done / total）
- 统计卡片实时更新：总计 / 建议复用 / 需新增
- 筛选按钮：全部 / 建议复用 / 需新增（点击后表格即时筛选，按钮高亮切换）
- 结果表格列：# / 商品名称 / 单位 / 结果 / 建议编码 / 匹配逻辑说明
- 表格固定高度 380px，overflow-y 滚动，表头 sticky
- 结果逐条追加到表格（不等全部完成），滚动条自动跟随
- 重置按钮（仅影响模块B）清空索引、结果、状态，回到 Step1
- 关闭浏览器或退出程序后索引自动释放，下次启动必须重新上传商品库

**FR-403：建议编码悬停气泡**

- 鼠标悬停在"建议编码"列的编码上 → 右侧弹出白色气泡
- 气泡内容：编码 / 名称 / 单位 / 分类 / 品牌 / 规格（来自库中候选的详细信息）
- 气泡样式：白底 + 灰色边框 + 阴影，左侧三角箭头指向编码
- 不在表格单元内渲染气泡 DOM，避免被滚动容器裁切

**FR-404：设置弹窗**

- 点击顶部栏 ⚙ 设置按钮 → 居中弹出模态弹窗
- 三个字段，全部可手动修改：
  - API 地址：默认值 `https://api.deepseek.com/v1`
  - API Key：密码输入框，占位文字 sk-xxxx
  - 模型名：默认值 `deepseek-chat`
- 保存按钮关闭弹窗并更新 judge 模块的 API_KEY（热生效）
- 取消按钮关闭弹窗不保存

**FR-405：日志面板（开发阶段）**

- 页面底部固定一行日志面板（黑色背景，绿色等宽字体）
- 时间线格式：`[HH:MM:SS] 消息内容`
- 颜色区分：绿色(普通) / 黄色(警告) / 红色(错误) / 青色(系统)
- 自动捕获：window.onerror、console.error、未处理的 Promise 拒绝
- 业务代码通过 `_log(msg, color)` 埋点
- 面板最大高度 200px，超出滚动
- 正式发布时可通过开关关闭或隐藏

**FR-406：标签页切换**

- 两个标签页：清洗去重 / 新品查重
- 默认显示"清洗去重"
- 点击标签切换页面，当前标签高亮（白色下划线），另一个半透明
- 切换时不丢失各自的状态（模块A的结果切到B再切回来还在）
- 切标签时后台任务继续运行不中断（模块B查重中可以切换到模块A查看清洗结果）
- 每个模块的[重置]按钮只影响自己的模块，不影响另一个标签页

---

## 界面设计

### 模块A（清洗去重）布局

```
┌─ 顶部栏：[🧹清洗去重] [🔍新品查重]           ⚙设置 ─┐
├─ 步骤条                                               ─┤
│  [1.上传商品库] → [2.自动清洗] → [3.导出结果]          │
├─ 上传区：点击或拖拽上传商品库 Excel                     ─┤
├─ 进度条：清洗中... 5000/12440 ████████░░               ─┤
├─ 统计卡片：(总条数|发现重复|正常|缺省值)                  ─┤
├─ [📥导出] [🔄重置]                                     ─┤
├─ 结果表格（滚动）                                       ─┤
│  SPUID | 名称 | 单位 | 描述 | 异常分类 | 组号 | 建议    │
└────────────────────────────────────────────────────────┘
```

### 模块B（新品查重）布局

```
┌─ 顶部栏：[🧹清洗去重] [🔍新品查重]           ⚙设置 ─┐
├─ 步骤条                                               ─┤
│  [1.上传商品库] → [2.上传接龙表] → [3.导出]             │
├─ Step1 上传区：上传商品库 Excel                         ─┤
├─ 建索引进度条                                          ─┤
├─ 就绪提示：✅ 索引就绪：11702 条，30 个品类              ─┤
├─ Step2 上传区（锁定/激活联动）                           ─┤
├─ [▶开始] [⏸暂停] [⏹停止] [🔄重置] [📥导出]            ─┤
├─ 进度条：查重中... 45/646 ████████░░                    ─┤
├─ 统计卡片：(总计|建议复用|需新增)                         ─┤
├─ 筛选：[全部] [建议复用] [需新增]                         ─┤
├─ 结果表格（380px高，滚动，表头sticky）                    ─┤
│  # | 商品名称 | 单位 | 结果 | 建议编码 | 匹配逻辑说明     │
└────────────────────────────────────────────────────────┘
├─ 日志面板 (开发阶段)                                     ─┤
└────────────────────────────────────────────────────────┘
```

---

## 技术方案

### 技术选型

| 层 | 选型 | 理由 |
|----|------|------|
| 后端 | Python Flask | 三期已有，二期引擎直接 import |
| 前端 | HTML + 内嵌 CSS/JS | 无构建工具，打包友好 |
| 模块A引擎 | 一期 scripts/rules.py | 纯规则，不调模型 |
| 模块B引擎 | 二期 scripts/matcher.py + judge.py | 规则找候选 + DeepSeek 判断 |
| 打包 | PyInstaller | 三期已验证 |

### 数据流

```
模块A: 商品库.xlsx → rules.py(RuleEngine) → 结果表格
模块B: 商品库.xlsx → translator + build_index → index dict
       接龙表.xlsx → check_one(translator, ..., index) → judge() → DeepSeek → 结果表格
```

### 改动范围

| 文件 | 改动 |
|------|------|
| `web/app.py` | 新增 /api/clean（模块A）+/api/build-index（模块B Step1），task_state 加 index 字段 |
| `web/templates/index.html` | 替换为两个标签页布局 + 日志面板 |
| `scripts/rules.py` | 不动（直接 import 用） |
| `scripts/matcher.py` | 不动 |
| `scripts/judge.py` | 不动 |

---

## 风险

| 风险 | 缓解 |
|------|------|
| 模块A 清洗 12440 条可能较慢（~30秒） | 进度条 + 每 200 条刷新 |
| 模块B DeepSeek API 调用慢（2秒/条） | 异步处理，逐条刷新 |
| 模块A/B 同时运行时内存占用 | 单标签页设计，切换时保持各自状态但不并行 |

## 成功标准

- [ ] 模块A 完整流程：上传 → 清洗进度 → 结果表格 → 导出
- [ ] 模块B 完整流程：上传商品库 → 建索引 → 上传接龙表 → 查重 → 结果 → 导出
- [ ] Step2 锁定联动：未建索引时灰色不可点
- [ ] 暂停/停止按钮状态机正确
- [ ] 筛选按钮即时生效
- [ ] 编码悬停气泡显示完整信息
- [ ] 设置弹窗保存生效
- [ ] 日志面板显示所有操作时间线

## 验收方式

1. 模块A：上传 商品库整理.xlsx → 看进度条 → 看统计 → 看表格(颜色标签) → 导出
2. 模块B：上传 商品库整理_结果.xlsx → 建索引 → 上传接龙表 → 开始 → 暂停/继续/停止 → 筛选 → 悬停编码 → 导出
3. 重置：点重置 → 状态清空 → 回到初始
4. 公共：打开设置弹窗 → 修改 API Key → 保存 → 生效

---
*基于 demo_四期.html 定稿，日期：2026-05-27*
